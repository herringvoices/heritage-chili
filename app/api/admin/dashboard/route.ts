import { getDb } from "@/db"; import { requireAdminRequest } from "@/server/admin-request"; import { dataResponse, errorResponse } from "@/server/http"; import { AdminDashboardQueryService } from "@/server/services/admin-dashboard-query-service";
export async function GET(request: Request) { try { await requireAdminRequest(request); return dataResponse(await new AdminDashboardQueryService(getDb()).getAdminDashboard()); } catch (error) { return errorResponse(error); } }
